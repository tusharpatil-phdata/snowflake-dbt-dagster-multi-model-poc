name: snowflake_dbt_dagster_poc
version: 1.0.0
config-version: 2

profile: snowflake_poc

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
snapshot-paths: ["snapshots"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  snowflake_dbt_dagster_poc:
    +materialized: table

    staging:
      +tags: ["staging"]

    intermediate:
      +tags: ["intermediate"]

    marts:
      +tags: ["mart"]
